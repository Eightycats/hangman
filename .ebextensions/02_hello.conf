files:
   /tmp/00_slim_config.conf:
      mode: "000644"
      owner: root
      group: root
      content: |

    location / {
        try_files $uri /index.php$is_args$args;
    }

container_commands:
  02_reload_nginx:
    command: "sudo echo hello""
